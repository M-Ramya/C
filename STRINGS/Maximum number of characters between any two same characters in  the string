#include<stdio.h>
#include<string.h>

int main()
{
	char s[12];
	int l,i,j,count,max=0;
	scanf("%s",s);
	l=strlen(s);
	for(i=0;i<l;i++)
	{
		for(j=i+1;j<l;j++)
		{
		if(s[i]==s[j])
{
	count=j-i-1;
}
	}
	if(max<count)
	{
		max=count;
	}
}
printf("%d",max);
return 0;
}
