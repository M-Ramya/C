#include <stdio.h>

int main() {
    int size,i,j,arr[20];
    scanf("%d",&size);
    for(i=0;i<size;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=0;i<size;i++)
    {
        for(j=0;j<size;j++)
        {
            if((arr[i]==arr[j])&&(i!=j))
            {
                break;
            }
                    }
                    if(j==size)
                    {
                        printf("%d ",arr[i]);
                    }
    }
}
